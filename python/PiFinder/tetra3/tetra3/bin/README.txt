This directory should be populated with an executable named
'cedar-detect-server'. This binary is a gRPC server that implements the
CedarDetect service declared at ../proto/cedar_detect.proto.

The binary should be built from the Rust source at
https://github.com/smroid/cedar-detect.
